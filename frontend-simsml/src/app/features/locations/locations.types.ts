export interface Location {
    locationId: number;
    code: string;
    name: string;
    active: boolean;
}

export interface Locationsummary {
    locationId: number;
    code: string;
    name: string;
}